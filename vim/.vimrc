"	adds line numbers to the left of each line
set number

"	turns syntax hilighting on.
syntax on

"	This is the colorscheme for the syntax: choices installed : simple-dark and molokai
colorscheme molokai

"	This sets the tab to 4 instead of the outrageous one of vim
set tabstop=4

"	To check if i like it. 
set autoindent

"	If you want to replace tabs with spaces uncomment the following line
"	set expendtab

"	Hilights the current line :)
set cursorline
